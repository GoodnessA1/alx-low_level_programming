#include <stdio.h>
#include "main.h"

/**
 * main - Prints the name of the file
 * @argc: argument count
 * @argv: argument variable
 *
 * Return: 0(successful)
 */
int main(int argc, char **argv)
{
	puts(argv[0]);
	return (0);
}
